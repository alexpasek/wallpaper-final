These PDFs were moved out of `public/` because Cloudflare Pages rejects static assets larger than 25 MiB.

If any of these files need to be served on the site, they should be compressed below the limit or hosted outside the Pages asset bundle.
